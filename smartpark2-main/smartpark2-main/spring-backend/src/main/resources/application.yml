spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartpark
    username: postgres
    password: '#L123456y'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

app:
  jwt:
    secret: "change-this-secret"
    expirationMillis: 86400000

